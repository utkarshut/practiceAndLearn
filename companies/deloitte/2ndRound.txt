
EMPLOYEE  firstName lastName empid

Count of duplicate firstname 
 Name, count

 Select firstName, count(*) from Employee GroupBy firstName having count(*) > 1;


  Int a = 7;
  Int b = 8;
  
    A = A + B ;
    B =  A  - b
    A =  A - b;

String s = “ababacdca”; 
First non repeating char 
     s.chars().mapToObject(c -> (char)c)
                   .collect(Collectors.groupingBy(c->c,LinkedHashMap::new, Collectors.counting())
                   .entrySet().stream()
                   .filter(f-> f.getValues()==1)
                   .map(Map.Entry::geKeys)
                   .stream().findFirst().orElse(“”);
